"use strict";

const constants  = require('../constants');

constants.error.rest = {
  renderIndex: 'Error al renderizar inicio',
  renderForm: 'Error al renderizar formulario',
  renderLogin: 'Error al renderizar login',
  renderForget: 'Error al renderizar forget',
  renderRecovery: 'Error al renderizar recovery',
  renderAdmin: 'Error al renderizar admin',
  renderPoliticasPrivacidad: 'Error al renderizar políticas de privacidad',
  renderCondicionesServicio: 'Error al renderizar condiciones de servicio',
  renderFormAdmin: 'Error al renderizar formulario de administrador',
}

module.exports = constants;